
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function isAuthenticated() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return request.auth.uid == userId;
    }

    match /users/{userId} {
      allow read, list: if isAuthenticated();
      allow write: if isOwner(userId);
    }

    match /predictions/{userId} {
      // Allow any authenticated user to list the collection, but only the owner to read a single document.
      // This is necessary for the consensus page to function.
      allow list: if isAuthenticated();
      allow read: if isAuthenticated() && isOwner(userId);
      allow write: if isOwner(userId);
    }
    
    match /playerTeamScores/{scoreId} {
        allow read, list: if isAuthenticated();
        // The document ID is a combination of userId and teamId, so we can't rely on it for ownership.
        // We must check the userId field inside the document itself.
        allow create, update, delete: if isAuthenticated() && isOwner(request.resource.data.userId);
    }

    match /userHistories/{userId} {
        allow read, list: if isAuthenticated();
        allow write: if isOwner(userId);
    }

    // Public collections that anyone can read.
    match /teams/{teamId} {
      allow read, list: if isAuthenticated();
      allow write: if false; 
    }

    match /standings/{standingId} {
      allow read, list: if isAuthenticated();
      allow write: if false;
    }

    match /previousSeasonStandings/{standingId} {
      allow read, list: if isAuthenticated();
      allow write: if false;
    }

     match /weeklyTeamStandings/{standingId} {
      allow read, list: if isAuthenticated();
      allow write: if false;
    }

    match /teamRecentResults/{resultId} {
      allow read, list: if isAuthenticated();
      allow write: if false;
    }

    match /monthlyMimoM/{awardId} {
      allow read, list: if isAuthenticated();
      allow write: if false;
    }
    
    match /seasonMonths/{monthId} {
      allow read, list: if isAuthenticated();
      allow write: if false;
    }

    match /matches/{matchId} {
      allow read, list: if isAuthenticated();
      allow write: if false;
    }
  }
}
