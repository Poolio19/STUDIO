rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // WARNING: Insecure for production. This allows anyone to read/write to your database.
      // This was set to unblock development. Rules should be secured before launch.
      allow read, write: if true;
    }
  }
}
